framework:
  secret: fos
  router:
    resource: "%kernel.root_dir%/config/routing.yml"
  test: ~

fos_http_cache:
  proxy_client:
    varnish:
      servers: 127.0.0.1
  rules:
    -
      match:
        path: ^/cachetest/cached
      headers:
        cache_control: {public: true}

monolog:
  handlers:
    main:
      type: stream
      level: debug
